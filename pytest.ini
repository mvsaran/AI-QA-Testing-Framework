[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    smoke: Fast availability and latency checks against the RAG API.
    regression: Quality and behavior checks for the RAG API.
    contract: API contract and schema validation checks.
    negative: Negative and malformed-response validation checks.
